# Informe de Seguridad SPECTRA

**ID de Ejecución:** `{{ run.id }}`
**Objetivo:** `{{ run.target_url }}`
**Framework:** {{ run.framework or "Desconocido" }}
**Estado:** {{ run.status }}
**Generado:** {{ now }}

---

## Resumen

| Métrica | Valor |
|---------|-------|
| Total de Eventos | {{ run.total_events }} |
| Hallazgos | {{ run.findings_count }} |
| Puntuación Blast Radius | {{ "%.2f"|format(run.blast_radius_score or 0) }} |
| Persistencia Detectada | {{ "Sí ⚠️" if run.persistence_detected else "No ✓" }} |
| Iniciada | {{ run.started_at or "—" }} |
| Completada | {{ run.completed_at or "—" }} |

---

## Hallazgos por Severidad

{% for sev, items in findings_by_severity.items() %}
### {{ sev | upper }} ({{ items | length }})

{% for f in items %}
#### Hallazgo — `{{ f.event_type }}`

- **Clasificación:** {{ f.classification }}
- **Severidad:** {{ f.severity }}
- **Nodo:** {{ f.node_id or "N/D" }}
- **Timestamp:** {{ f.timestamp }}

**Payload enviado:**
```
{{ (f.payload_sent or "") | truncate(500) }}
```

**Respuesta (fragmento):**
```
{{ (f.response_received or "") | truncate(500) }}
```

**Indicadores:**
{% for ind in (f.event_metadata.get("indicators", [])) %}
- `{{ ind }}`
{% endfor %}

---
{% endfor %}
{% else %}
No se detectaron hallazgos.
{% endfor %}

## Blast Radius

{% if run.blast_radius_detail %}
- **Puntuación:** {{ "%.2f"|format(run.blast_radius_score or 0) }}
- **Nodos afectados:** {{ run.blast_radius_detail.get("affected_nodes", []) | join(", ") }}
- **Profundidad en cascada:** {{ run.blast_radius_detail.get("cascade_depth", 0) }}
{% else %}
No calculado.
{% endif %}

## Detección de Persistencia

{% if run.persistence_detail %}
- **Detectada:** {{ run.persistence_detail.get("persisted", False) }}
- **Indicadores:** {{ run.persistence_detail.get("indicators", []) | join(", ") or "Ninguno" }}
{% else %}
No verificado.
{% endif %}

---

*Generado por SPECTRA — Plataforma de Seguridad para Agentes IA*

# SPECTRA Security Report

**Run ID:** `{{ run.id }}`
**Target:** `{{ run.target_url }}`
**Framework:** {{ run.framework or "Unknown" }}
**Status:** {{ run.status }}
**Generated:** {{ now }}

---

## Summary

| Metric | Value |
|--------|-------|
| Total Events | {{ run.total_events }} |
| Findings | {{ run.findings_count }} |
| Blast Radius Score | {{ "%.2f"|format(run.blast_radius_score or 0) }} |
| Persistence Detected | {{ "Yes ⚠️" if run.persistence_detected else "No ✓" }} |
| Started | {{ run.started_at or "—" }} |
| Completed | {{ run.completed_at or "—" }} |

---

## Findings by Severity

{% for sev, items in findings_by_severity.items() %}
### {{ sev | upper }} ({{ items | length }})

{% for f in items %}
#### Finding — `{{ f.event_type }}`

- **Classification:** {{ f.classification }}
- **Severity:** {{ f.severity }}
- **Node:** {{ f.node_id or "N/A" }}
- **Timestamp:** {{ f.timestamp }}

**Payload sent:**
```
{{ (f.payload_sent or "") | truncate(500) }}
```

**Response (excerpt):**
```
{{ (f.response_received or "") | truncate(500) }}
```

**Indicators:**
{% for ind in (f.event_metadata.get("indicators", [])) %}
- `{{ ind }}`
{% endfor %}

---
{% endfor %}
{% else %}
No findings detected.
{% endfor %}

## Blast Radius

{% if run.blast_radius_detail %}
- **Score:** {{ "%.2f"|format(run.blast_radius_score or 0) }}
- **Affected nodes:** {{ run.blast_radius_detail.get("affected_nodes", []) | join(", ") }}
- **Cascade depth:** {{ run.blast_radius_detail.get("cascade_depth", 0) }}
{% else %}
Not computed.
{% endif %}

## Persistence Detection

{% if run.persistence_detail %}
- **Detected:** {{ run.persistence_detail.get("persisted", False) }}
- **Indicators:** {{ run.persistence_detail.get("indicators", []) | join(", ") or "None" }}
{% else %}
Not checked.
{% endif %}

---

*Generated by SPECTRA — AI Agent Security Platform*
